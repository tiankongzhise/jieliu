# 通用 AI 协作约束

本文件是跨项目复用的 AI 协作记忆与开发约束。任何 AI 助手、自动化代理或开发者在修改项目之前，都应先读取本文件，并结合项目本地文档、真实代码和 Git 历史判断上下文。

## 开发前上下文

- 开始任务前必须读取本文件、项目本地 `AGENTS.md`、相关项目文档、近期 Git 历史和待修改代码，不要只根据假设修改。
- 读取包含中文的项目资料前，先确认编码能正确支持中文；如出现乱码，需要用 UTF-8 等支持中文的编码重新读取或修正读取方式。
- 涉及架构、授权、凭据、外部 API、迁移、部署、测试链路或历史能力时，必须结合 Git 历史和权威文档判断，不能只看当前文件快照。
- 遇到问题并解决后，将可复用的根因、解决方案和后续约束沉淀到项目本地 `AGENTS.md`，防止后续 AI 重复犯错。

## 文档权威与同步

- 优先遵守项目本地已声明的权威文档路径和优先级。
- 如果项目没有既定文档体系，默认维护 `docs/PRD.md`、`docs/DESIGN.md`、`docs/TECH.md`、`docs/SPEC.md`、`AGENTS.md` 和 `docs/DEVELOPMENT_PLAN.md`。
- 初始化项目、用户新增需求或用户变更需求时，先细化用户目标、用户角色、核心流程、约束、验收标准和影响范围，再同步更新相关文档。
- 五类核心项目文档应遵循“最少充分、可验证、可交接”的开发惯例：详细到另一名开发者或 AI 不依赖口头上下文即可实现、测试和追踪；避免重复代码、堆砌背景或写无法验证的空泛描述。
- `docs/PRD.md` 应明确用户目标、用户角色、业务场景、范围内/范围外、功能需求、非功能约束、验收标准、依赖、风险、待确认问题和 deferred 项；以用户可感知行为为主，不写不必要的实现方案。
- `docs/DESIGN.md` 应明确产品体验、信息架构、核心流程、页面/组件或交互状态、空/错/加载/权限等状态、可访问性和视觉约束；无前端或交互影响时，也要写明“无影响”及原因。
- `docs/TECH.md` 应明确架构决策、模块边界、数据模型、接口契约、外部依赖、配置与环境、迁移/部署、安全与隐私、性能、观测日志、失败处理、官方文档依据和关键取舍。
- `docs/SPEC.md` 应把 PRD 需求拆成可实现、可测试的功能规格，明确输入输出、状态流转、校验规则、错误处理、权限、边界条件、数据字段、接口或事件契约，并能追溯到对应 PRD 条目。
- `docs/DEVELOPMENT_PLAN.md` 应明确任务拆分、执行顺序、依赖关系、影响文件或模块、负责人/执行者、状态、验证命令、测试覆盖、文档更新、阻塞点和 deferred 后续项。
- `AGENTS.md` 应只沉淀跨任务可复用的协作约束、项目惯例、命令、环境坑点和安全边界；不要把一次性需求、产品规格或临时计划写入其中。
- PRD 中的有效功能需求应能在 SPEC 或开发计划中找到对应任务，或明确标记为 deferred。
- 如果新增或变更需求对设计、技术或 AI 协作约束无影响，应在 SPEC 或开发计划中说明无影响及原因。
- 更新任一核心文档后，需要检查其他相关文档是否仍一致，并同步维护开发排期或状态记录。

## Git 历史与提交

- 每次进入新阶段或开始修改前，阅读最近提交历史；建议至少查看 `git log --oneline --decorate -n 20`。
- 当前工作依赖近期能力时，阅读相关关键提交的 `git show --stat --summary --format=fuller <commit>`。
- 实现需求、修复问题、更新文档、同步排期或修改本文件后，应创建清晰的 Git commit。
- Commit message 应使用项目主要语言；中文项目使用中文，并说明本次变化、技术细节、影响范围和验证结果。
- 不要把多个无关功能合并到一个模糊提交中。
- 不要提交构建产物、缓存、临时文件或无关格式化变更。


## 后端、数据与外部 API

- 后端实现必须保留来源证据、可信度、待人工确认状态、错误信息和可追踪日志，不能把不确定数据包装成确定结论。
- 数据源导入、采集、解析、分类、生成和渲染应分层实现，避免把网络请求、解析逻辑和内容生成耦合在同一不可测试流程中。
- 涉及外部 API、SDK、云服务或依赖库接口时，必须优先获取官方文档并记录实现依据。
- 如果无法获取官方文档，必须明确说明已经尝试的方式和当前实现依据，不得假装已经核验。
- 必要数据缺失或不可验证时，应阻塞依赖任务，或在 SPEC、开发计划和状态报告中标记待补全。

## 前端体验

- 业务工具类前端应优先呈现用户当前可执行的工作区，不做无必要的装饰性落地页。
- 界面布局应保持信息清晰、可扫描，并把来源、原文、解析结果、人工确认状态和错误信息靠近展示。
- 不确定状态不能只依赖颜色表达，必须使用明确文本或图标，例如“待确认”“字段缺失”“来源不可达”“解析失败”。
- 新增页面、组件、交互或视觉生成物时，应同步更新设计文档；若无前端影响，应在 SPEC 或开发计划中标注。

## 测试、构建与沙箱

- 运行测试、构建或工具命令失败时，先判断失败来自项目代码、环境权限、缓存、网络、工具链还是路径编码。
- 沙箱或本地工具遇到编码、路径、权限、命令行为、依赖、测试、构建、打包问题时，解决后应记录到项目本地 `AGENTS.md`。
- 中文项目在 PowerShell 中读取中文文本时，应显式使用 UTF-8；仅设置控制台编码不等于文件读取编码正确。
- 运行外部 skill 自带校验脚本时，不要假设全局 Python 已安装脚本依赖；如果项目没有对应依赖管理上下文，优先使用该 skill 声明的运行方式或无依赖校验，不要用裸 `pip` 临时安装。
- Codex 沙箱中 `git fetch` 可能因无法写入 `.git/FETCH_HEAD` 报 `Permission denied`；若该 fetch 对同步或开发判断必要，应按沙箱权限问题处理并提权重试同一条 `git fetch`，不能误判为远端或分支不存在。
- 测试或构建产生的 `.cache/`、`.venv/`、`dist/`、临时目录和二进制产物不得提交，除非项目文档明确要求。

## 敏感信息

- 不得提交 `.env`、真实数据库连接串、access token、refresh token、用户密码、密钥文件、本地数据库、明文 manifest、本地缓存文件或日志中的敏感信息。
- `.env.example` 只能包含占位示例，不得包含真实密钥。
- 调试或联调中产生的真实凭据必须脱敏记录，不能写入仓库。
